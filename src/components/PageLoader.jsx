import React from 'react';

/**
 * PageLoader Component
 * Full-screen spinner overlay hidden per user request ("일단 스피너는 감춰").
 */
export function PageLoader({ fullScreen = true }) {
  return null;
}

export default PageLoader;
