/**
 * Admin data access — single import for all admin pages.
 * Components import from here.
 *
 * All requests route directly to real HTTP API (adminApiService.js).
 */

export * from './api/adminApiService.js';
